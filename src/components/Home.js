const Home = (props) => {
	// const { msgAlert, user } = props
	console.log('props in home', props)

	return (
		<>
		<div className="splashPic"></div>
			<h2>Home Page</h2>
		</>
	)
}

export default Home
